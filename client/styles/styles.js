/**
 * Created by Vitaliy on 02.11.2016.
 */

const main = {
  display: 'flex',
  flexDirection: 'column'
};

const mainContainer = {
  maxWidth: 980,
  paddingTop: 100,
  margin: '0 auto'
};

const container = {
  padding: '1.5em',
  backgroundColor: 'white',
  display: 'flex',
  flexDirection: 'column'
};

const addButton = {
  position: 'fixed',
  bottom: '1em',
  right: '1em'
};

export { main, container, mainContainer, addButton };
